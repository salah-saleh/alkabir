name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Build image (verifies Dockerfile/nginx config are valid)
        run: docker build -t alkabir-ci-check .
